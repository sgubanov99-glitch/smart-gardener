// core/placement.js — алгоритм размещения (заглушка, будет наполнен)
export const GAP = 0.5;
export function findSpotFor(scheme, type, culture, extra) {
  // TODO (следующий этап): зоны по солнцу, зазоры, проверка света
  return { x: 0, y: 0, w: 1, l: 1 };
}